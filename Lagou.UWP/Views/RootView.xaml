<Page
    x:Class="Lagou.UWP.Views.RootView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Lagou.UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:cm="using:Caliburn.Micro"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <ContentControl cm:View.Model="{Binding SearchBarVM}" HorizontalContentAlignment="Stretch" />

        <!--<Frame Grid.Row="1" cm:Message.Attach="[Event Loaded] = [Action FrameLoaded($source)]">-->
        <Frame Grid.Row="1" x:Name="frm">

        </Frame>
    </Grid>
</Page>
